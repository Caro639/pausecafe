{% extends 'base.html.twig' %}

{% block title %}Réinitialiser son mot de passe{% endblock %}

{% block body %}
<section class="container" id="form_login">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <h1 class="text-center" id="h1_login">Réinitialiser son mot de passe</h1>

            {{ form_start(resetPasswordForm)}}
            {{ form_row(resetPasswordForm.password) }}
            <button class="btn btn-lg btn-dark mt-3" type="submit">
                Valider
            </button>
             <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}">
            {{ form_end(resetPasswordForm) }}

            {# <form method="post" action="{{ path('app_forgot_password_request') }}">
                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('reset_password_request') }}"
                >
                <label for="inputEmail">Email</label>
                <input type="email" name="email" id="inputEmail" class="form-control" autocomplete="email" required>
                <button class="btn btn-lg btn-dark mt-3" type="submit">
                    Valider
                </button>

{# <form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Vous êtes connecté(e) {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Me déconnecter</a>
        </div>
    {% endif %}

    <h2 class="h3 mb-3 font-weight-normal">Réinitialiser mon mot de passe</h2>
    <label for="inputEmail">Email</label>
    <input type="email" name="email" id="inputEmail" class="form-control" autocomplete="email" required>
    <label for="inputPassword">Mot de passe</label>
    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required> #}

    {# <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    > #}

    {# <div class="checkbox mb-3">
        <label>
            <input type="checkbox" name="_remember_me"> Remember me
        </label>
    </div>

    <button class="btn btn-lg btn-dark mt-3" type="submit">
        Valider
    </button>
</form> #}
   </div>
</div>
</section>
{% endblock %}
